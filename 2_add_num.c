// program to add two numbers 
#include <stdio.h>

int main()
{
    int a,b;
    printf("Enter two numbers: ");
    scanf("%d %d",&a,&b);
    printf("Sum of the two numbers is : %d ",a+b);
    return 0;
}